_target_: lattice_lab.data.EBMDataModule

train_parquet: 01_preprocessing/processed_bindingdb/threshold_90/train.parquet
val_parquet: 01_preprocessing/processed_bindingdb/threshold_90/val.parquet
protein_store: 03_protein_encoder/embeddings/esm2_650M/
decoy_store: 04_ebm_head/decoy_zm_ssl2/
bdb_store: 04_ebm_head/bdb_zm_ssl2

batch_size: 64
n_decoys: ${n_decoys}
hard_mining_mult: 1
binders_only: true
binder_threshold_nm: null

# Hard-negative decoy mix (BDB other-target binders + annotated non-binders).
frac_other_binder: 0.4
frac_non_binder: 0.15

# Fixed validation mix (decouples val/* from training decoys). null = reuse train mix.
val_decoy_store: null
val_bdb_store: null
val_frac_other_binder: 0.4
val_frac_non_binder: 0.15

# MMseqs2 cluster-weighted protein sampling.
cluster_weighted_sampler: false
cluster_min_identity: 0.5
cluster_cache_dir: 05_training/checkpoints

num_workers: 0
seed: ${seed}
